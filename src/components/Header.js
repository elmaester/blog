import { Link } from "gatsby";
import * as React from "react";

const Header = () => (
  <header className="header">
    <div className="set-global-width">
      <Link to="/">Oleg.Date Blog</Link>
    </div>
  </header>
);

export default Header;
